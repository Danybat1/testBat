package com.freightops.enums;

public enum InvoiceType {
    CLIENT, // Facture client
    SUPPLIER, // Facture fournisseur
    TRANSPORT, // Facture transport
    PASSENGER, // Facture passager
    ADDITIONAL_SERVICES, // Services additionnels
    MIXED // Facture mixte (plusieurs types)
}
